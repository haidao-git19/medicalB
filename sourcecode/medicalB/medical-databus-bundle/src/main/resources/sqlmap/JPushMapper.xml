<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.netbull.shop.manage.weixin.dao.JPushDao">

    <insert id="savePushLogInfo" parameterType="java.util.Map">
		INSERT INTO tb_medical_push_log (aliases,aliasesType,alert,sender,senderType,status,createTime,bizType,bizId) 
		VALUES(#{aliases},#{aliasesType},#{alert},#{sender},#{senderType},#{status},now(),#{bizType},#{bizId})
	</insert>
	
    <select id="queryPushLogList" parameterType="java.util.Map" resultType="java.util.Map">
		 <choose>
            <when test="type !=null and type ==1">
				select * from tb_medical_push_log p where p.aliases = CONCAT('YS_',#{ownerId}) order by p.createTime desc
            </when>
            <otherwise>
                select * from tb_medical_push_log p where p.aliases = CONCAT('HZ_',#{ownerId}) order by p.createTime desc
            </otherwise>
        </choose>
	</select>
	
</mapper>