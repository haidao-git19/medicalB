<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.netbull.shop.manage.weixin.dao.ImageDao">

    <select id="queryList" parameterType="map" resultType="com.netbull.shop.common.vo.ImageVo">
        select * from tb_product_goods_image 
         <where>
            <if test="goodsCode !=null and goodsCode !=''">
                goodsCode=#{goodsCode}
            </if>
        </where>
    </select>
    
    <insert id="save" parameterType="com.netbull.shop.common.vo.ImageVo" 
        useGeneratedKeys="true" keyProperty="id">
         INSERT tb_product_goods_image(goodsCode,bigImgPath,smallImgPath,sort) 
         VALUES(#{goodsCode},#{bigImgPath},#{smallImgPath},#{sort})
    </insert>
    
    <delete id="delete" parameterType="java.lang.Long">
        delete from tb_product_goods_image 
        <where>
            <if test="_parameter !=null and _parameter !=''">
                id=#{value}
            </if>
        </where>
    </delete>
	
</mapper>